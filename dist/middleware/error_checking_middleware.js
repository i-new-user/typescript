"use strict";
// import { Request, Response, NextFunction } from "express";
// import { validationResult, ValidationError } from "express-validator";
// import { }
// export const errorCeckingMiddliware = (req: Request, res: Response, next: NextFunction) => {
//     const errorFormat = ({msg, param}: ValidationError) => {
//         return {
//             message: msg, 
//             field: param
//         }
//     }
// }
// const result = validationResult(req).formatWith(errorFormater)
// if(!result.isEmpty()){
//     res.status(400).json(errorMessages: result.array())
// } else {
//     next()
// }
